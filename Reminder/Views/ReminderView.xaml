<UserControl x:Class="Reminder.Views.ReminderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ViewModels="clr-namespace:Reminder.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance ViewModels:ReminderViewModel}"
             mc:Ignorable="d">
    <StackPanel>
        <TextBlock Text="{Binding Reminder.Task}"/>
        <TextBlock Text="{Binding Reminder.RepeatPeriod, StringFormat={}{0:c}}"/>
        <TextBlock Text="{Binding RemainingTime, StringFormat={}{0:c}}"/>
    </StackPanel>
</UserControl>